import { AppendDirective } from './append.directive';

describe('AppendDirective', () => {
  it('should create an instance', () => {
    const directive = new AppendDirective();
    expect(directive).toBeTruthy();
  });
});
