import { PrependDirective } from './prepend.directive';

describe('PrependDirective', () => {
  it('should create an instance', () => {
    const directive = new PrependDirective();
    expect(directive).toBeTruthy();
  });
});
